{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe{% endblock %}

{% block body %}
    <h1 class="text-center"> Changement de mot de passe <h1>

    {% if changed == true %}    
        <div class="alert alert-success"> {{notification }} </div>
    {% else %}
        <div class="alert alert-danger "> {{notification }} </div>
    {% endif %}

    {{ form(form)}}

    <a href="{{ path('app_account')}}"> Revenir à mon compte </a>
{% endblock %}
